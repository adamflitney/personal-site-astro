---
import Navigation from "../components/Navigation.astro";
import Footer from "../components/Footer.astro";
import "../styles/global.css";

const pageTitle = "About Me";

const identity = {
  firstName: "Adam",
  country: "UK",
  occupation: "Software Engineer",
  hobbies: ["watching anime", "reading", "coding", "playing WoW"],
};

const technicalSkills = [
  "HTML",
  "CSS",
  "Javascript",
  "Typescript",
  "React",
  "NodeJS",
  "AWS",
];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <style define:vars={{ skillColor, fontWeight, textCase }}>
      h1 {
        color: purple;
        font-size: 4rem;
      }
      .skill {
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        text-transform: var(--textCase);
      }
    </style>
  </head>
  <body>
    <Navigation />
    <h1>{pageTitle}</h1>
    <p>
      I'm an experienced senior full-stack software engineer with a passion for
      problem solving and personal development.
    </p>
    <pre>"A jack of all trades is a master of none, but oftentimes better than a master of one" -Some amazing person</pre>

    <p>
      I'm currently rebuilding my personal website using Astro. Ever since I saw
      astro being used on a learnWithJason youtube video, I've been enchanted by
      its simple yet powerful approach to web development.
    </p>

    <p>Here are some facts about me:</p>
    <ul>
      <li>My name is {identity.firstName}</li>
      <li>
        I live in {identity.country} and I work as a {identity.occupation}.
      </li>
      {
        identity.hobbies.length >= 2 && (
          <li>
            Two of my hobbies are: {identity.hobbies[0]} and{" "}
            {identity.hobbies[1]}
          </li>
        )
      }
    </ul>
    <p>My primary technical skills are:</p>
    <ul>
      {technicalSkills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>

    {happy && <p>I am happy to be learning Astro!</p>}
    {finished && <p>I finished this tutorial!</p>}
    {
      goal === 3 ? (
        <p>My goal is to finish in 3 days.</p>
      ) : (
        <p>My goal is not 3 days.</p>
      )
    }
    <Footer />
  </body>
</html>
